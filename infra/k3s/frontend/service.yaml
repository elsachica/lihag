apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 8001         # Porten som exponeras i klustret
      targetPort: 3000   # Porten som containern lyssnar på
  type: ClusterIP