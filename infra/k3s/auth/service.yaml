apiVersion: v1
kind: Service
metadata:
  name: auth
spec:
  selector:
    app: auth
  ports:
    - port: 8000         # Porten som exponeras i klustret
      targetPort: 8000   # Porten som containern lyssnar på
  type: ClusterIP