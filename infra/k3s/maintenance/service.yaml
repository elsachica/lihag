apiVersion: v1
kind: Service
metadata:
  name: maintenance
spec:
  selector:
    app: maintenance
  ports:
    - port: 8888         # Porten som exponeras i klustret
      targetPort: 8888   # Porten som containern lyssnar på
  type: ClusterIP