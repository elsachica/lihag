apiVersion: v1
kind: Service
metadata:
  name: notification
spec:
  selector:
    app: notification
  ports:
    - port: 8888         # Porten som exponeras i klustret
      targetPort: 8888   # Porten som containern lyssnar på
  type: ClusterIP